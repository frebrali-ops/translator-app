# Railway usa Nixpacks; este ficheiro define como arrancar a app
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT backend.app:app"
